import axios from "axios"

export const shortenUrl = async (longUrl: string) => {
    // const response = await axios.post("url", {url: "13"}, {
    //     headers: {
    //       'Content-Type': 'application/json',
    //       Authorization: `Bearer 1233`
    //     }
    //   })

    return "cyphermoon.com"
}